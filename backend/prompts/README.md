# Prompts

Reserved for prompt template files used by the Business Layer and AI Gateway.

Per `CLAUDE.md`, prompts must not be hardcoded inside Python source files. This
directory is intentionally empty until prompt-driven features (question
generation, answer evaluation) are implemented.
