"""Knowledge Layer: file-based repositories for prompts and interview questions.

Per ARCHITECTURE.md section 7(vi), distinct from the AI Gateway - this
layer only stores and looks up content, it never calls an AI provider.
"""
